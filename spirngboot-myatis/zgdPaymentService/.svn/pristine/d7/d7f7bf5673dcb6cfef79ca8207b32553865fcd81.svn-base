<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ule.uhj.init.dao.ConstantMapper" >
	
  <select id="query" parameterType="map" resultType="com.ule.uhj.init.modle.Constant" >
    select element_key elementKey,
    	   element_value elementValue,
    	   remark,
    	   status 
    from ULEAPP_FINANCECR.t_j_constant
    where element_key =#{key,jdbcType=VARCHAR}
  </select>

  <update id="update" parameterType="map" >
  	update ULEAPP_FINANCECR.t_j_constant c 
  	set c.element_value= #{value,jdbcType=VARCHAR}, 
  		c.status=#{status,jdbcType=VARCHAR} 
  	where c.element_key = #{key,jdbcType=VARCHAR}
  </update>
	
  <select id="queryFace" parameterType="map" resultType="map" >
    select USER_ONLY_ID "userOnlyId",
    	   IMAGE_URL "imageUrl"
    from uleapp_financecr.t_j_apply_image
    where image_type =#{imagetype,jdbcType=VARCHAR}
  </select>
  <insert id="insertSql" parameterType="String">
        ${value}
  </insert>
  <update id="updateSql" parameterType="String">
        ${value}
  </update>
  <delete id="deleteSql" parameterType="String">
        ${value}
  </delete>
</mapper>